import React from 'react';
// import LiveStudent from '../LiveStudent/LiveStudent';

class SharkTank extends React.Component {
  render() {
    // const swimmers = studentsData.livingStudents();

    // const swimmerCards = swimmers.map((swimmer) => (
    //     <LiveStudent key={swimmer.id} />
    // ));
    return (

    <div>
      <h2>I am the pool</h2>
      {/* { swimmerCards } */}
    </div>
    );
  }
}

export default SharkTank;
